# Contracts

This folder contains testnet-only Arc helper contracts for simulation workflows.

- `ArcTradeIntentLedger.sol` logs user-confirmed testnet trade intents
- `interfaces/IERC20USDC.sol` provides a minimal ERC-20 interface for future extensions

These contracts do not custody funds and do not auto-execute swaps.
